version: 2
name: bunny-hop
development:
  command: [python3, server.py]
  health: /healthz
release:
  adapter: compose
  compose: q5m/compose.yaml
  contract: q5m/app.env
  health: /healthz
environments:
  staging:
    default_node: q5m-n01
